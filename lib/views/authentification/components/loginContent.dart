import 'package:flutter/widgets.dart';

class LoginContent extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: [Text("AUTHENTIFICATION")],
    );
  }
}
